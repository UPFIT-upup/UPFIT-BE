services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=upfit'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=upfit'
    ports:
      - '3306:3306'
